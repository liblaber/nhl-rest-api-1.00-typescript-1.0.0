// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const awayTeam1 = z.object({
  id: z.number().optional(),
  name: z.string().optional(),
  link: z.string().optional(),
});

/**
 *
 * @typedef  {AwayTeam1} awayTeam1
 * @property {number}
 * @property {string}
 * @property {string}
 */
export type AwayTeam1 = z.infer<typeof awayTeam1>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const awayTeam1Response = z
  .object({
    id: z.number().optional(),
    name: z.string().optional(),
    link: z.string().optional(),
  })
  .transform((data) => ({
    id: data['id'],
    name: data['name'],
    link: data['link'],
  }));

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const awayTeam1Request = z
  .object({ id: z.number().nullish(), name: z.string().nullish(), link: z.string().nullish() })
  .transform((data) => ({
    id: data['id'],
    name: data['name'],
    link: data['link'],
  }));
